version: 2

sources:
  - name: snowflake_raw
    database: raw
    schema: jaffle_shop
    tables:
      - name: orders
      - name: customers

  - name: snowflake_raw2
    database: raw
    schema: stripe
    tables:
      - name: payment

